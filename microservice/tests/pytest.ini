[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    --verbose
    --tb=short
    --strict-markers

markers =
    unit:        fast, isolated — no I/O, no real services
    integration: exercises the full HTTP stack via FastAPI TestClient